node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# --- render-farm: NO subir assets pesados ni binarios al repo público ---
# (los assets viajan como release asset; el workflow recrea public/ al renderizar)
public/
whisper.cpp/
*.log
*.wav
*.mp4
*.mov
*.webm
*.MP3
rtmp/
chunks/
*.tar
img_index*.json
vid_index*.json
.gh_token2

# venv + cuda build + binarios pesados (NO trackear — inflan el push)
ttsenv/
whisper_cuda/
youtube/meta/*.png
*.tar

# temp/working dirs (matchfarm, fetch, diag, stills)
_match/
_pubshow/
_fill*/
_diag/
_w[0-9]*
_slice*.json
_wlog*.txt
_check_*.png
_assets_list.txt
lista_*.txt
_*_tts*.log
cookies/_*/

# Proxies residenciales + cookies de cuentas (credenciales, se inyectan en runtime)
cookies/proxies.txt
cookies/*.txt
nock/

# Credenciales de Telegram (bot token + chat_id) — NUNCA commitear
.telegram_bot
telegram_bot*
*.telegram

# Telegram userbot (Telethon) — sesión y credenciales, NUNCA commitear
.tg_docs.session
*.session
*.session-journal
.tg_userbot
tg_creds.txt
.tg_delivery
.tg_bridge_state.json
.tg_bridge.log
